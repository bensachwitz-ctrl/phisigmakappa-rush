# Some platform integrations (Clerk, Stream, Cloudinary) declare peer ranges that
# npm's strict resolver rejects against this app's pinned Next 14 / React 18.
# legacy-peer-deps lets the install resolve (matches the local install + what the
# CI/Vercel build needs) without forcing unwanted major upgrades.
legacy-peer-deps=true
